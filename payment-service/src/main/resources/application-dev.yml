logging.config: classpath:log4j2-spring-dev.xml

spring:
  config:
    import:
      - ./actuator/app-dev-actuator.yml
      - ./api/app-dev-api.yml
      
app:
  info:
    environment: ${spring.profiles.active}
