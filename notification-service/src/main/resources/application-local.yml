logging.config: classpath:log4j2-spring-local.xml

spring:
  config:
    import:
      - ./actuator/app-local-actuator.yml
      - ./mail/app-local-mail.yml
      
app:
  info:
    environment: ${spring.profiles.active}
